import { MathJax, MathJaxContext } from 'better-react-mathjax'

#### `mean(input: a JS array, matrix or tensor)`

**param** `input` a JS array, matrix or tensor, or a series of numbers

**returns** - a number representing the mean (average) of `input`

This function simply finds the mean of some numbers

<iframe src="https://hlab.app/s/LinJianan/mean_book"
    width="100%"
    height="500px">
</iframe>
