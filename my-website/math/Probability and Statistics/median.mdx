import { MathJax, MathJaxContext } from 'better-react-mathjax'

#### `median(input: a JS array, matrix or tensor)`

**param** `input` a JS array, matrix or tensor, or a series of numbers

**returns** - a number representing the median of `input`

This function simply finds the median of some numbers

<iframe src="https://hlab.app/s/LinJianan/median_book"
    width="100%"
    height="500px">
</iframe>
