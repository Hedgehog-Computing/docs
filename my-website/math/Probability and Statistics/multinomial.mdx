import { MathJax, MathJaxContext } from 'better-react-mathjax'

#### `median(input: a JS array, matrix or tensor)`

**param** `input` a JS array, matrix or tensor, or a series of numbers

**returns** - a number representing the multinomial of `input`

This function simply finds the multinomial of some numbers according to the following formula

<MathJaxContext>
      <MathJax>{"$$\\text{multinomial}(x) = \\frac{(x_1 + x_2 + ... + x_n)!}{x_1!\\cdot x_2!\\cdot ... \\cdot x_n!}$$"}</MathJax>
</MathJaxContext>

<iframe src="https://hlab.app/s/LinJianan/multinomial_book"
    width="100%"
    height="500px">
</iframe>
